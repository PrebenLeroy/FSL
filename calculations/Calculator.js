class Calculator
{

    calculateDurability(averageKilometerPerTrip, averageRefueling, maxRefStops, averageUse, caloricValue,  maxCO2){
        let refPertrip = averageKilometerPerTrip / averageRefueling;
        if(refPertrip <= (maxRefStops + 1) &&  <= maxCO2){

        }
    }

    calculateAverageLoadingstops(totalKilometerYear, averageKilometerPerTrip,
         averageLoadingStops,  totalLoadingCapacityYear)
     {
        return totalLoadingCapacityYear/ (totalKilometerYear / averageKilometerPerTrip) / averageLoadingStops;
     }

     
} export class {Calculator}
